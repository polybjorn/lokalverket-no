# The site is a work-in-progress holding page and carries a noindex meta tag.
# Crawling is allowed on purpose: a crawler has to fetch the page to see the
# noindex. "Disallow: /" would block the fetch and leave older, indexed
# versions of the page sitting in search results.
User-agent: *
Allow: /

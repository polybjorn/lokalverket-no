# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# asset library symlink (-> ~/Vault/Lokalverket/Brand)
assets

# dev-only design reference: symlink -> ~/Vault/Lokalverket/Brand/Brand guide/.
# Deliberately untracked - this repo is public and everything under src/pages/
# is served at lokalverket.no, so it must never reach a CI checkout.
src/pages/styleguide.astro

# jetbrains setting folder
.idea/

# claude code (CLAUDE.md is tracked, local settings are not)
.claude/

# secrets and credentials
*.pem
*.key
*.kdbx
id_rsa
id_ed25519
credentials.json
